import Generator from 'yeoman-generator'

const dynamicloadedcacheMap = new Map()

export default function dynamicLoadGenerators(framework: string, context: Generator) {
  if (dynamicloadedcacheMap.get(framework)) {
    
  }
}